
# SPRING
spring:
  # DATA SOURCE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://42.192.15.218:3306/handshaker?serverTimezone=UTC
    username: qiong
    password: qiong
    type: com.alibaba.druid.pool.DruidDataSource
  # REDIS
  redis:
    host: 42.192.15.218 #localhost #
    port: 6379
    client-type: lettuce
    lettuce:
      pool:
        max-active: 8
  main:
    banner-mode: off

  mvc:
    static-path-pattern: /uploads/**

  web:
    resources:
      static-locations: classpath:/document/

my:
  interview-header: uploads
  document-folder: document

# MYBATIS
mybatis-plus:
  global-config:
    db-config:
      id-type: auto
    banner: false
  configuration:
    # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: false # 关闭 驼峰
  mapper-locations: classpath:mapper/*Mapper.xml,classpath:mapper/**/*Mapper.xml

# LOG

server:
  port: 80


